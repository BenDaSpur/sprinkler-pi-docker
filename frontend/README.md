# Sprinkler Pi Frontend
### Built with [svelte](https://svelte.dev)

## Setup
- Set variables inside .env
- run `npm install`

## Running
- run `npm run dev` to see site in dev mode
- open site on `http://localhost:5000` and password is `spurlock`
- run `npm run build` to build a static version to serve through nginx/pm2